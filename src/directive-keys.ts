const directiveKeys = {
  attribute: "x-attr",
  closest: "x-closest",
  each: "x-each",
  intersect: "x-intersect",
  interval: "x-interval",
  label: "x-label",
  state: "x-state",
  value: "x:",
};

export default directiveKeys;
